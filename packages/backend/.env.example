# =============================================
# VTG TOOL - Backend Environment Configuration
# =============================================
# Copy this file to .env and fill in your values
# NEVER commit .env file to git!

# ============== REQUIRED SETTINGS ==============

# Database Connection (PostgreSQL)
# Example: postgresql+asyncpg://username:password@host:port/database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/vtgtool

# Security - JWT Secret Key (REQUIRED - Generate a strong random key!)
# Generate using: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_TO_A_SECURE_RANDOM_STRING_32_CHARS_OR_MORE

# ============== OPTIONAL SETTINGS ==============

# Environment: development, staging, production
ENVIRONMENT=development

# Redis Cache URL
REDIS_URL=redis://localhost:6379

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# File Upload Configuration
UPLOAD_DIR=uploads
MAX_FILE_SIZE=104857600

# ============== PRODUCTION SETTINGS ==============
# For production deployment, use:
# ENVIRONMENT=production
# DATABASE_URL=postgresql+asyncpg://user:pass@production-db:5432/vtgtool
# REDIS_URL=redis://production-redis:6379
# SECRET_KEY=<secure-random-key-generated-with-secrets.token_urlsafe(32)>

# ============== SECURITY NOTES ==============
# 1. Always use a strong, unique SECRET_KEY in production
# 2. Never expose database credentials in code
# 3. Use HTTPS in production (ENVIRONMENT=production enables secure cookies)
# 4. Rotate SECRET_KEY periodically for enhanced security
# 5. Keep .env file permissions restricted (chmod 600 .env)
